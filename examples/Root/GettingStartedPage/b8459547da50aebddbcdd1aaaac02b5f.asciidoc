[source, csharp]
----
var searchResponse = client.Search<Account>(s => s
    .Index("bank")
    .MatchAll()
    .Source(sf => sf
        .Includes(ff => ff
            .Field(f => f.AccountNumber)
            .Field(f => f.Balance)
        )
    )
);
----
