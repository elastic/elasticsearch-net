[source, csharp]
----
var searchResponse = client.Search<Account>(s => s
    .Index("bank")
    .Query(q => q
        .Bool(b => b
            .Must(mu => mu
                .Match(m => m
                    .Field(f => f.Address)
                    .Query("mill")
                ), mu => mu
                .Match(m => m
                    .Field(f => f.Address)
                    .Query("lane")
                )
            )
        )
    )
);
----
