[source, csharp]
----
var searchResponse = client.Search<Account>(s => s
    .Index("bank")
    .Query(q => q
        .Bool(b => b
            .Should(sh => sh
                    .Match(m => m
                        .Field(f => f.Address)
                        .Query("mill")
                    ), sh => sh
                    .Match(m => m
                        .Field(f => f.Address)
                        .Query("lane")
                    )
            )
        )
    )
);
----
