{
  "indices.delete_alias": {
    "url": {
      "paths": ["/{index}/_alias/{name}"],
      "deprecated_paths" : [
        {
          "version" : "7.0",
          "path" : "/{index}/_aliases/{name}",
          "description" : "_aliases is merely an alias and no longer used to refer to a single alias"
        }
      ]
    }
  }
}
