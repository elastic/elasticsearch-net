<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="searchable_snapshots.clear_cache.Request"><summary><para>
Clear the cache.
</para>
<para>
Clear indices and data streams from the shared cache for partially mounted indices.
</para></summary></member>
  <member key="searchable_snapshots.clear_cache.Request#allow_no_indices"><summary><para>
A setting that does two separate checks on the index expression.
If <c>false</c>, the request returns an error (1) if any wildcard expression
(including <c>_all</c> and <c>*</c>) resolves to zero matching indices or (2) if the
complete set of resolved indices, aliases or data streams is empty after all
expressions are evaluated. If <c>true</c>, index expressions that resolve to no
indices are allowed and the request returns an empty result.
</para></summary></member>
  <member key="searchable_snapshots.clear_cache.Request#expand_wildcards"><summary><para>
Whether to expand wildcard expression to concrete indices that are open, closed or both
</para></summary></member>
  <member key="searchable_snapshots.clear_cache.Request#ignore_unavailable"><summary><para>
If <c>false</c>, the request returns an error if it targets a concrete (non-wildcarded)
index, alias, or data stream that is missing, closed, or otherwise unavailable.
If <c>true</c>, unavailable concrete targets are silently ignored.
</para></summary></member>
  <member key="searchable_snapshots.clear_cache.Request#index"><summary><para>
A comma-separated list of data streams, indices, and aliases to clear from the cache.
It supports wildcards (<c>*</c>).
</para></summary></member>
</doc>