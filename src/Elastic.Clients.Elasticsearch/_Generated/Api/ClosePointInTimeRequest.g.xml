<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="_global.close_point_in_time.Request"><summary><para>
Close a point in time.
</para>
<para>
A point in time must be opened explicitly before being used in search requests.
The <c>keep_alive</c> parameter tells Elasticsearch how long it should persist.
A point in time is automatically closed when the <c>keep_alive</c> period has elapsed.
However, keeping points in time has a cost; close them as soon as they are no longer required for search requests.
</para></summary></member>
  <member key="_global.close_point_in_time.Request#id"><summary><para>
The ID of the point-in-time.
</para>
<para>
IMPORTANT: Each search request against a PIT returns in its response a <c>pit_id</c> field which may be different from the identifier you originally supplied.
Always use the most recently-received PIT identifier for the next request.
If you make concurrent search requests against the same PIT, Elasticsearch can return several different <c>pit_id</c> values in its responses.
In that case, use any of these values for later requests, preferring more recently-received values whenever possible.
</para></summary></member>
</doc>