<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="indices.get.Request"><summary><para>
Get index information.
</para>
<para>
Get information about one or more indices. For data streams, the API returns information about the
stream’s backing indices.
</para></summary></member>
  <member key="indices.get.Request#allow_no_indices"><summary><para>
A setting that does two separate checks on the index expression.
If <c>false</c>, the request returns an error (1) if any wildcard expression
(including <c>_all</c> and <c>*</c>) resolves to zero matching indices or (2) if the
complete set of resolved indices, aliases or data streams is empty after all
expressions are evaluated. If <c>true</c>, index expressions that resolve to no
indices are allowed and the request returns an empty result.
</para></summary></member>
  <member key="indices.get.Request#expand_wildcards"><summary><para>
Type of index that wildcard expressions can match. If the request can target data streams, this argument
determines whether wildcard expressions match hidden data streams. Supports comma-separated values,
such as open,hidden.
</para></summary></member>
  <member key="indices.get.Request#features"><summary><para>
Return only information on specified index features
</para></summary></member>
  <member key="indices.get.Request#flat_settings"><summary><para>
If true, returns settings in flat format.
</para></summary></member>
  <member key="indices.get.Request#ignore_unavailable"><summary><para>
If <c>false</c>, the request returns an error if it targets a concrete (non-wildcarded)
index, alias, or data stream that is missing, closed, or otherwise unavailable.
If <c>true</c>, unavailable concrete targets are silently ignored.
</para></summary></member>
  <member key="indices.get.Request#include_defaults"><summary><para>
If true, return all default settings in the response.
</para></summary></member>
  <member key="indices.get.Request#index"><summary><para>
Comma-separated list of data streams, indices, and index aliases used to limit the request.
Wildcard expressions (*) are supported.
</para></summary></member>
  <member key="indices.get.Request#local"><summary><para>
If true, the request retrieves information from the local node only. Defaults to false, which means information is retrieved from the master node.
</para></summary></member>
  <member key="indices.get.Request#master_timeout"><summary><para>
Period to wait for a connection to the master node. If no response is received before the timeout expires, the request fails and returns an error.
</para></summary></member>
</doc>