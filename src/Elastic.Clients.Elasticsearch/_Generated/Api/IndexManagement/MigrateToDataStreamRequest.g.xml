<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="indices.migrate_to_data_stream.Request"><summary><para>
Convert an index alias to a data stream.
Converts an index alias to a data stream.
You must have a matching index template that is data stream enabled.
The alias must meet the following criteria:
The alias must have a write index;
All indices for the alias must have a <c>@timestamp</c> field mapping of a <c>date</c> or <c>date_nanos</c> field type;
The alias must not have any filters;
The alias must not use custom routing.
If successful, the request removes the alias and creates a data stream with the same name.
The indices for the alias become hidden backing indices for the stream.
The write index for the alias becomes the write index for the stream.
</para></summary></member>
  <member key="indices.migrate_to_data_stream.Request#master_timeout"><summary><para>
Period to wait for a connection to the master node. If no response is received before the timeout expires, the request fails and returns an error.
</para></summary></member>
  <member key="indices.migrate_to_data_stream.Request#name"><summary><para>
Name of the index alias to convert to a data stream.
</para></summary></member>
  <member key="indices.migrate_to_data_stream.Request#timeout"><summary><para>
Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
</para></summary></member>
</doc>