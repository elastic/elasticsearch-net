<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="indices.add_block.Request"><summary><para>
Add an index block.
</para>
<para>
Add an index block to an index.
Index blocks limit the operations allowed on an index by blocking specific operation types.
</para></summary></member>
  <member key="indices.add_block.Request#allow_no_indices"><summary><para>
If <c>false</c>, the request returns an error if any wildcard expression, index alias, or <c>_all</c> value targets only missing or closed indices.
This behavior applies even if the request targets other open indices.
For example, a request targeting <c>foo*,bar*</c> returns an error if an index starts with <c>foo</c> but no index starts with <c>bar</c>.
</para></summary></member>
  <member key="indices.add_block.Request#block"><summary><para>
The block type to add to the index.
</para></summary></member>
  <member key="indices.add_block.Request#expand_wildcards"><summary><para>
The type of index that wildcard patterns can match.
If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.
It supports comma-separated values, such as <c>open,hidden</c>.
</para></summary></member>
  <member key="indices.add_block.Request#ignore_unavailable"><summary><para>
If <c>false</c>, the request returns an error if it targets a missing or closed index.
</para></summary></member>
  <member key="indices.add_block.Request#index"><summary><para>
A comma-separated list or wildcard expression of index names used to limit the request.
By default, you must explicitly name the indices you are adding blocks to.
To allow the adding of blocks to indices with <c>_all</c>, <c>*</c>, or other wildcard expressions, change the <c>action.destructive_requires_name</c> setting to <c>false</c>.
You can update this setting in the <c>elasticsearch.yml</c> file or by using the cluster update settings API.
</para></summary></member>
  <member key="indices.add_block.Request#master_timeout"><summary><para>
The period to wait for the master node.
If the master node is not available before the timeout expires, the request fails and returns an error.
It can also be set to <c>-1</c> to indicate that the request should never timeout.
</para></summary></member>
  <member key="indices.add_block.Request#timeout"><summary><para>
The period to wait for a response from all relevant nodes in the cluster after updating the cluster metadata.
If no response is received before the timeout expires, the cluster metadata update still applies but the response will indicate that it was not completely acknowledged.
It can also be set to <c>-1</c> to indicate that the request should never timeout.
</para></summary></member>
</doc>