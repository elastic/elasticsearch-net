<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="indices.clear_cache.Request"><summary><para>
Clear the cache.
Clear the cache of one or more indices.
For data streams, the API clears the caches of the stream's backing indices.
</para>
<para>
By default, the clear cache API clears all caches.
To clear only specific caches, use the <c>fielddata</c>, <c>query</c>, or <c>request</c> parameters.
To clear the cache only of specific fields, use the <c>fields</c> parameter.
</para></summary></member>
  <member key="indices.clear_cache.Request#allow_no_indices"><summary><para>
If <c>false</c>, the request returns an error if any wildcard expression, index alias, or <c>_all</c> value targets only missing or closed indices.
This behavior applies even if the request targets other open indices.
</para></summary></member>
  <member key="indices.clear_cache.Request#expand_wildcards"><summary><para>
Type of index that wildcard patterns can match.
If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.
Supports comma-separated values, such as <c>open,hidden</c>.
</para></summary></member>
  <member key="indices.clear_cache.Request#fielddata"><summary><para>
If <c>true</c>, clears the fields cache.
Use the <c>fields</c> parameter to clear the cache of specific fields only.
</para></summary></member>
  <member key="indices.clear_cache.Request#fields"><summary><para>
Comma-separated list of field names used to limit the <c>fielddata</c> parameter.
</para></summary></member>
  <member key="indices.clear_cache.Request#ignore_unavailable"><summary><para>
If <c>false</c>, the request returns an error if it targets a missing or closed index.
</para></summary></member>
  <member key="indices.clear_cache.Request#index"><summary><para>
Comma-separated list of data streams, indices, and aliases used to limit the request.
Supports wildcards (<c>*</c>).
To target all data streams and indices, omit this parameter or use <c>*</c> or <c>_all</c>.
</para></summary></member>
  <member key="indices.clear_cache.Request#query"><summary><para>
If <c>true</c>, clears the query cache.
</para></summary></member>
  <member key="indices.clear_cache.Request#request"><summary><para>
If <c>true</c>, clears the request cache.
</para></summary></member>
</doc>