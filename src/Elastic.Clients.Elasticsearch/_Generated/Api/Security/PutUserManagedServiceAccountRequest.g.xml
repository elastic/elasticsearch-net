<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="security.put_user_managed_service_account.Request"><summary><para>
Create user-managed service accounts.
</para>
<para>
Create a service account in a namespace of your own, or replace one that already exists.
A replacement is not a partial update: every write applies the defaults, so an account that was disabled and is then written again without <c>enabled</c> comes back enabled.
</para>
<para>
Creating an account whose name still has leftover service tokens is rejected.
Delete those tokens first.
</para>
<para>
NOTE: The <c>elastic</c> namespace is reserved for the built-in service accounts that ship with Elasticsearch.
The <c>manage_service_account</c> privilege does not authorize this API.
</para></summary></member>
  <member key="security.put_user_managed_service_account.Request#enabled"><summary><para>
Whether the account can authenticate.
Tokens can still be created for a disabled account; they just cannot be used until the account is enabled.
</para></summary></member>
  <member key="security.put_user_managed_service_account.Request#namespace"><summary><para>
The namespace, which is a top-level grouping of service accounts.
It must start with a letter or digit and can contain only letters, digits, hyphens, and underscores, up to a maximum of 128 characters.
It cannot be <c>elastic</c>, which is reserved for built-in service accounts.
</para></summary></member>
  <member key="security.put_user_managed_service_account.Request#refresh"><summary><para>
If <c>wait_for</c> (the default) then wait for a refresh to make this operation visible to search, if <c>true</c> then refresh the affected shards to make this operation visible to search, if <c>false</c> then do nothing with refreshes.
</para></summary></member>
  <member key="security.put_user_managed_service_account.Request#roles"><summary><para>
The names of the roles to grant to the service account, up to a maximum of 1000.
The roles are resolved when the account authenticates, so they do not have to exist yet.
</para></summary></member>
  <member key="security.put_user_managed_service_account.Request#service"><summary><para>
The service name.
It must start with a letter or digit and can contain only letters, digits, hyphens, and underscores, up to a maximum of 128 characters.
</para></summary></member>
</doc>