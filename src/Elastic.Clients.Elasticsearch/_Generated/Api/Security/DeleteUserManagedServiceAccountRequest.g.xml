<?xml version="1.0" encoding="utf-8"?>
<doc>
  <member key="security.delete_user_managed_service_account.Request"><summary><para>
Delete user-managed service accounts.
</para>
<para>
Delete a service account from a namespace of your own.
</para>
<para>
Deleting an account that still has service tokens is rejected unless <c>force</c> is <c>true</c>.
A forced delete leaves the tokens behind: they cannot authenticate while no account of that name exists, and recreating the account is rejected until they are deleted.
</para>
<para>
NOTE: The <c>elastic</c> namespace is reserved for the built-in service accounts that ship with Elasticsearch.
A name that no user-managed service account could have is rejected rather than reported as not found.
The <c>manage_service_account</c> privilege does not authorize this API.
</para></summary></member>
  <member key="security.delete_user_managed_service_account.Request#force"><summary><para>
If <c>false</c> (the default), deleting a service account that still has service tokens is rejected.
If <c>true</c>, the account is deleted and its tokens are left in place.
</para></summary></member>
  <member key="security.delete_user_managed_service_account.Request#namespace"><summary><para>
The namespace, which is a top-level grouping of service accounts.
It must start with a letter or digit and can contain only letters, digits, hyphens, and underscores, up to a maximum of 128 characters.
It cannot be <c>elastic</c>, which is reserved for built-in service accounts.
</para></summary></member>
  <member key="security.delete_user_managed_service_account.Request#refresh"><summary><para>
If <c>wait_for</c> (the default) then wait for a refresh to make this operation visible to search, if <c>true</c> then refresh the affected shards to make this operation visible to search, if <c>false</c> then do nothing with refreshes.
</para></summary></member>
  <member key="security.delete_user_managed_service_account.Request#service"><summary><para>
The service name.
It must start with a letter or digit and can contain only letters, digits, hyphens, and underscores, up to a maximum of 128 characters.
</para></summary></member>
</doc>